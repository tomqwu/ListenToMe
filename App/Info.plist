<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <!-- Debug builds override this to "ListenToMe (Dev)" so local builds and the released
         app are distinguishable in the Dock and in System Settings → Privacy & Security,
         where they hold separate permission grants. CFBundleName gets the same value because
         some Privacy panes (e.g. Screen & System Audio Recording) label rows with the bundle
         NAME, not the display name — without this, both apps' rows read "ListenToMe" there.
         PRODUCT_NAME is untouched, so the bundle is ListenToMe.app in both configs.
         See project.yml. -->
    <key>CFBundleDisplayName</key>
    <string>$(BUNDLE_DISPLAY_NAME)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(BUNDLE_DISPLAY_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSMinimumSystemVersion</key>
    <string>26.0</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>ListenToMe transcribes meeting audio you choose to capture.</string>
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>ListenToMe converts captured speech to text on your device.</string>
    <key>NSCalendarsFullAccessUsageDescription</key>
    <string>ListenToMe reads your calendar only to pre-fill meeting context when you ask. Like your other notes, it goes to whichever model you pick — a local model keeps it on your Mac; a cloud model receives it.</string>
</dict>
</plist>
